<%= form_for(@gift) do |f| %>
  <% if @gift.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gift.errors.count, "error") %> prohibited this gift from being saved:</h2>

      <ul>
      <% @gift.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title, "Nimi" %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description, "Kuvaus" %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :age, "Ikä" %><br>
    <%= f.number_field :age %>
  </div>
  <div class="field">
    <%= f.label :gender, "Sukupuoli" %><br>
    <%= f.text_field :gender %>
  </div>
  <%= f.hidden_field :user_id, :value => current_user.id%> 
  <div class="actions">
    <%= f.submit "Lisää lahjavinkki" %>
  </div>
<% end %>
